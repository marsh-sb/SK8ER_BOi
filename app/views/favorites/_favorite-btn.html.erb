<% if post.favorited_by?(current_user) %>
  いいね<%= link_to post.favorites.count, post_favorites_path(post), method: :delete, remote: true, class: "fas fa-heart heart_red" %>
<% else %>
  いいね<%= link_to post.favorites.count, post_favorites_path(post), method: :post, remote: true, class: "fas fa-heart heart_blue" %>
<% end %>